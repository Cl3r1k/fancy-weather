// const PaletteClass = require('../src/js/PaletteClass');

// describe('#hexToRGBA()', () => {
//   test(`it should return converted color from 'ffffff' to 'rgba(255, 255, 255, 255)'`, () => {
//     // Arrange
//     const paletteTest = new PaletteClass(null, null, 0, 0);

//     // Act

//     // Assert
//     expect(paletteTest.hexToRGBA('ffffff')).toEqual('rgba(255, 255, 255, 255)');
//   });

//   test(`it should return converted color from '000000' to 'rgba(0, 0, 0, 255)'`, () => {
//     // Arrange
//     const paletteTest = new PaletteClass(null, null, 0, 0);

//     // Act

//     // Assert
//     expect(paletteTest.hexToRGBA('000000')).toEqual('rgba(0, 0, 0, 255)');
//   });
// });
